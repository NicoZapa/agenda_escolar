<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>- Mis cursos -</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/estilos.css">
    <!--<link rel="stylesheet" href="css/styles.css">-->
</head>
<body>
    <header>
        <div class="container">
            <div class="logo">
                <p>Agenda Escolar</p>
            </div>

            <nav>
                <a href="#">Cerrar Sesion</a>
                <a href="#">¿Que es Agenda Escolar?</a>
                <a href="#">Mi Perfil</a>
                <a href="./index.html">Inicio</a>
            </nav>
        </div>
    </header>

    <section id="hero">

      <h2>EDICION DE NOTAS</h2>
      <% if(alumno) { %>
        <h3><%= alumno.nombre_materia %></h3>
        <form action="/update" method="POST">

            <input id="id" name="id"  value="<%= alumno.id_usuario %>" hidden>
            <input id="id_materia" name="id_materia"  value="<%= alumno.id_materia %>" hidden>
            
            <div class="mb-3">
              <label for="nota1" class="form-label">Nota 1º Trimestre: <%= alumno.calificacion_trimestre1 %></label>
              <input type="number" class="form-control" id="nota1" name="nota1" max="10" value="<%= alumno.calificacion_trimestre1%>">
            </div>

            <div class="mb-3">
              <label for="nota2" class="form-label">Nota 2º Trimestre: <%= alumno.calificacion_trimestre2 %></label>
              <input type="number" class="form-control" id="nota2" name="nota2" max="10" value="<%= alumno.calificacion_trimestre2 %>">
            </div>

            <div class="mb-3">
              <label for="nota3" class="form-label">Nota 3º Trimestre: <%= alumno.calificacion_trimestre3 %></label>
              <input type="number" class="form-control" id="nota3" name="nota3" max="10" value="<%= alumno.calificacion_trimestre3 %>">
            </div>

            <div class="mb-3">
              <label class="form-label">Alumno</label>
              <input type="text" class="form-control" id="exampleInputPassword1" value="<%= alumno.nombre %> <%= alumno.apellido %>" disabled>
            </div>

            <button type="submit" class="btn btn-primary">Submit</button>
        </form>

        <a href="/notas-cursos-docente/<%= alumno.id_materia %>">VOLVER</a>
      <% } %>
    </section>

    
</body>
</html>